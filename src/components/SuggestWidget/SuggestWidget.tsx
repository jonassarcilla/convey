import React from "react"

// export interface ISuggestWidgetProps {}
// {}: ISuggestWidgetProps

const SuggestWidget = () => {
  return <>SuggestWidget</>
}

export default React.memo(SuggestWidget)
